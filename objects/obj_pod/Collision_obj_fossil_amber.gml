/// @desc change to aerodactyl

//make egg, but skin it for pod
scp_breedpad_mate_function("gender", "same", 1,	1, , ,									"aerodactyl",		spr_pod_amber);

instance_destroy(other);
instance_destroy(self);